export function Card({
  className,
}: {
  className?: string;
  // title: string;
  // children: React.ReactNode;
  // href: string;
}): JSX.Element {
  return (
    <div
      className='bg-red-500'
    >

      this is from card
    </div>
  );
}
